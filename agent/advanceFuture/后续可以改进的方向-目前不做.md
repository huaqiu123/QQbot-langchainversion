不做的事（本轮范围外）
不提供知识库的手动编辑/删除接口 —— 仅支持追加写入
不提供文件上传/PDF 解析 —— 仅接收纯文本
不提供知识库的自动过期/清理机制
不做多租户/权限隔离 —— 整个进程共享一个知识库
不做 Embedding 模型微调
不安装本地 Embedding 模型（不引入 PyTorch / sentence-transformers）
不做 NapCat/OneBot 协议接入 —— 本轮只做 Agent V2 后端
